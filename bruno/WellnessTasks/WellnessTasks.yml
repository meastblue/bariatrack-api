info:
  name: WellnessTasks
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/graphql"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "query": "query WellnessTasks { wellnessTasks { id title description category durationMin isActive createdAt } }",
        "variables": {}
      }
  auth: inherit

settings:
  encodeUrl: true
