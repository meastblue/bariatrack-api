info:
  name: MySupplementLogs
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/graphql"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "query": "query { mySupplementLogs { id supplementId takenAt createdAt } }"
      }
  auth: inherit

settings:
  encodeUrl: true
