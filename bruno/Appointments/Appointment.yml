info:
  name: Appointment
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/graphql"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "query": "query Appointment($id: UUID!) { appointment(id: $id) { id patientId doctorId appointmentType title scheduledAt durationMin location teleconsultUrl status notes createdAt updatedAt } }",
        "variables": {
          "id": "00000000-0000-0000-0000-000000000000"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
