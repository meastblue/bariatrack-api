info:
  name: MyDietPhaseHistory
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/graphql"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "query": "query MyDietPhaseHistory { myDietPhaseHistory { id phaseId patientId status startedAt endedAt createdAt } }",
        "variables": {}
      }
  auth: inherit

settings:
  encodeUrl: true
