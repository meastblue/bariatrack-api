info:
  name: SkipDietPhase
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/graphql"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "query": "mutation SkipDietPhase($id: UUID!) { skipDietPhase(id: $id) { id status } }",
        "variables": {
          "id": "{{diet_phase_id}}"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
